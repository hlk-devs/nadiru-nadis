# nadi-translate

**Translate text between languages. Works with any language the underlying models support.**

A focused single-purpose Nadi that demonstrates the simplest useful pattern in Nadiru: send a structured prompt, display the result, let the Conductor handle routing. Translation is a task where most modern models perform similarly well, so the Conductor reliably picks an efficient model and you get fast, cheap translations.

## Usage
